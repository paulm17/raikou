import { css } from "@stylefusion/react";

export const BackgroundImageRootStyle = css({
  backgroundSize: "cover",
  backgroundPosition: "center",
  display: "block",
  width: "100%",
  border: 0,
  textDecoration: "none",
  borderRadius: "var(--bi-radius, 0)",
});
