import type { FlexFactory } from "@raikou/server";
import type { StylesApiData } from "../types";

export const FlexStylesApi: StylesApiData<FlexFactory> = {
  selectors: {
    root: "Root element",
  },

  vars: {},
};
